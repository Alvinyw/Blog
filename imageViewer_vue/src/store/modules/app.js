import * as session from '../../utils/session'

export default {
  namespaced: true,
  state: {
    merchantId: '1207619872164040706', // 一链数云 商户ID
  },
  mutations: {
    // updateState(state, param) {
    //   state.childMerchantId = param.cm ? param.cm : ""
    // },
  },
  actions: {
    // 初始化应用
    // async setMerchantInfo(commit, param) {
    //   const query = param ? param : {}
    //   commit('updateState', query)
    // },

  },
}